/* import firebase from "firebase/compat/app";

interface UserDoc {
	id: string;
	firstName: string;
	lastName: string;
	email: string;
	phoneNumber?: string;
	avatarUrl?: string;
	createdAt: firebase.firestore.Timestamp;
	// Outros campos necessários para representar o documento do usuário em seu aplicativo
}

export default UserDoc;
 */